export interface PlayerPosition {
    x: number;
    y: number;
}

export interface PlayerMovement {
    x: number;
    y: number;
}

export interface PlayerConfig {
    radius: number;
    speed: number;
}