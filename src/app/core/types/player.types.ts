export type PlayerPosition = {
    x: number;
    y: number;
};

export type PlayerMovement = {
    x: number;
    y: number;
};

export type PlayerConfig = {
    radius: number;
    speed: number;
};