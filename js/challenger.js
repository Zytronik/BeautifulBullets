import { CHALLENGER_SPRITE } from "./spriteSettings.js";

class Challenger {
    constructor(x, y){
        this.x = x;
        this.y = y;
        this.sprite = CHALLENGER_SPRITE.url
        this.size = 
    }
}