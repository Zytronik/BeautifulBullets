

export class marker{
    constructor(x, y, visuals) {
        this.x = x;
        this.y = y;
        this.visuals = visuals;
        this.bossCanvas = document.querySelector(".boss .characterCanvas");
        this.challengerCanvas = document.querySelector(".challenger .characterCanvas");
    }

    updateMarker() {
        
    }

    circularMarker(x, y, radius) {
        
    }
}